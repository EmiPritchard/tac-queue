{
  "name": "tac-support-queue",
  "version": "1.0.0",
  "description": "Access4 TAC support queue dashboard (UK and ANZ service desks)",
  "main": "server.js",
  "scripts": {
    "start": "node --use-system-ca --env-file-if-exists=.env server.js",
    "start:noenv": "node --use-system-ca server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-session": "^1.19.0"
  },
  "private": true,
  "engines": {
    "node": ">=22.9"
  }
}
